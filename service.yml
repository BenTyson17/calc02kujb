apiVersion: v1
kind: Pod
metadata: 
  name: calc-pod
  labels:
    project: calc
spec:
  containers: 
    - name: objective_driscoll
      image: ben17ly/calc
      ports:
        - containerPort: 5000
